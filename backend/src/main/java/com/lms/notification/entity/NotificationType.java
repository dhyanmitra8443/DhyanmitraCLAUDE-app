package com.lms.notification.entity;

/** Ref: SRS Chapter 14 - the notifications.notification_type CHECK constraint values (delivery channel). */
public enum NotificationType {
    IN_APP,
    EMAIL
}
