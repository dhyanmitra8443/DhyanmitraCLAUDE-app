package com.lms.resource.entity;

/** Ref: SRS Chapter 8 - the lesson_resources.resource_type CHECK constraint values. */
public enum ResourceType {
    VIDEO,
    PDF,
    IMAGE,
    AUDIO,
    ZIP,
    EXTERNAL_LINK
}
