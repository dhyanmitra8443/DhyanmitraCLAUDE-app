package com.lms.subscription.entity;

/** Ref: SRS Chapter 9 - the subscriptions.status CHECK constraint values. */
public enum SubscriptionStatus {
    ACTIVE,
    EXPIRED,
    CANCELLED,
    PENDING
}
